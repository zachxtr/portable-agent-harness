# Policy Command Code Log — 2026-05-30

> Development progress log for Policy Command. Maintained by Data Finn (datafinn.com). Each session captures completed work, current progress, and next steps in language suitable for both stakeholders and development agents.

---

## Session: Legislative Search Quality and Worker Pipeline Architecture

**Date:** 2026-05-30  
**Commits:** `fd8434a7` — agentic safety fixes (no evidence-free answers after search; honest empty-after-tools; correct model in logs) _(2026-05-29 commit; validated in prod this session)_

### Completed

- Traced a bad “bicycles in Florida” assistant answer to the model summarizing without seeing search results — not weak search — and shipped harness fixes so answers stay tied to retrieved documents
- Confirmed a follow-up production turn lists real bill numbers with citations aligned to sources (no invented bill IDs)
- Agreed that finding “all bills on a topic” needs **platform-level discovery** (one ranked bill list with a known total), not more ad-hoc skill-only tools
- Drafted a worker pipeline architecture note: PREPARE → ASSEMBLE → GENERATE → VALIDATE stays; tool-first search is valid; discovery should prefetch in ASSEMBLE; show up to 20 bills and say when more matched
- Documented README/code drift (documented gates that no longer run) so the next implementation pass cleans one execution story

### In Progress

- Corpus bill discovery not implemented yet — design only (`memory/wip_worker_agent_pipeline`)
- Legislative search still returns document snippets, not a structured bill inventory the validator can fully trust

### Next Steps

- Align the RAP service README with real pipeline gates and execution modes
- Implement shared discovery retrieval, then wire the search skill (prefetch discovery + analyst truncation rules)
- Optional: fix production rerank permissions so search ordering is strongest

---

## Session: Worker skill configuration refactor and discovery pipeline

**Date:** 2026-05-30  
**Commits:** `3c36ebc9` — worker agent pipeline cleanup; `a54b4e97` — skill tuning alignment and discovery pipeline

### Completed
- Consolidated how legislative search skills are configured — one master switch (`response_mode`) replaces overlapping old settings so each skill clearly runs in discover, direct, or agentic mode
- Shipped platform bill discovery for topic search — the assistant can return a ranked bill inventory (up to 20) instead of only document snippets
- Removed brittle topic-specific filtering from code; discovery quality is now tuned through documented skill knobs (weights, score floors, query variants)
- Published a single reference for all skill configuration variables and aligned the three base legislative skills to the new model
- Added a Joshua skill to ask the local assistant API directly and pull turn logs — faster iteration without the full UI
- Cleaned internal pipeline plumbing so runtime behavior follows `response_mode` only (no parallel “strategy” concept)

### Next Steps
- Rebuild and smoke-test rap-service after the configuration refactor
- Use the new direct-call skill to tune search, review, and analysis skills against real queries and logs
- Remove or trim unused knobs on base skills (e.g. agentic-only settings on discover-mode search)
- Calibrate discovery precision on bicycle-style topic queries

---

## Session: Discover statutes, doc cleanup, and briefing backlog

**Date:** 2026-05-30  
**Commits:** `3947b1e0` — continued refinement and cleanup, `26008d28` — discovery tune for session-level topic search, `c485602a` — usage block fix

### Completed

- Extended legislative search discovery to **statutes** and **both** bill+statute inventories — the assistant can answer “what statutes” and “all legislation” questions with a ranked list, not only bills
- Wired **Focus** (session, statute year, bill/statute/both intent) so search scope follows what the user set in the UI instead of guessing from rewritten questions
- Audited daily briefing cards — confirmed server-side data loading is working; retired outdated planning docs now that behavior matches the product
- Consolidated Joshua agent documentation — moved superseded drafts to archive; **RAP service README** is now the canonical reference for worker pipeline and briefing behavior
- Clarified next briefing work: tracking card should report **bill movement events**; team card should match **policy profile activity log** rows shown on Team Activity

### Next Steps

- Implement briefing data parity (BillTrackingLog + policy profile update-log)
- Tune discovery precision and PA question rewriting for consistent search results
- Prod rerank permissions when ready

---

## Session: UI polish, backlog triage, and prod stability audit

**Date:** 2026-05-30  
**Commits:** `1962c7f27` — todo cleanup and New Chat focus if none other selected

### Completed

- Hid allies and opponents on policy profiles until real contacts are wired — backend fields unchanged so the feature can return without a data migration
- Audited all system admin pages and closed the production resource-drain incident — root cause was the Indexing page hammering storage on load; that fix is confirmed and the broader page audit is done
- Trimmed the Joshua backlog — retired completed worker-pipeline and indexing items; focused active list on briefs, discovery tuning, and collection pipeline follow-ups
- Added a **New Chat** action in Assistant Focus when no conversation is selected — one click starts a fresh chat instead of showing an empty “None” state

### Next Steps

- Briefs data parity — tracking card from bill movement logs; team card from policy profile activity log
- Discovery precision tuning on real queries
- Collection pipeline review — how scraped documents trigger indexing under load

---

## Session: UI polish, backlog triage, and prod stability audit

**Date:** 2026-05-30  
**Commits:** `1962c7f27` — todo cleanup and New Chat focus if none other selected

### Completed

- Hid allies and opponents on policy profiles until real contacts are wired — backend fields unchanged so the feature can return without a data migration
- Audited all system admin pages and closed the production resource-drain incident — root cause was the Indexing page hammering storage on load; that fix is confirmed and the broader page audit is done
- Trimmed the Joshua backlog — retired completed worker-pipeline and indexing items; focused active list on briefs, discovery tuning, and collection pipeline follow-ups
- Added a **New Chat** action in Assistant Focus when no conversation is selected — one click starts a fresh chat instead of showing an empty “None” state

### Next Steps

- Briefs data parity — tracking card from bill movement logs; team card from policy profile activity log
- Discovery precision tuning on real queries
- Collection pipeline review — how scraped documents trigger indexing under load

